# fl_inject
A Flutter code generator for multi mini-app architecture. Automatically scans the entire project and packages to discover both built-in (@MiniAppScreenEntry, @MiniAppDISetup) and user-defined custom annotations, then generates a centralized router mapping and one-time DI setup.
